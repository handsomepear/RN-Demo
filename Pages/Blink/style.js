import React from 'react'

import { StyleSheet } from 'react-native'

export default blink = StyleSheet.create({
  text : {
    fontSize: 20,
    color: 'lightblue',
    marginTop: 20,
    backgroundColor: 'purple'
  }
})

